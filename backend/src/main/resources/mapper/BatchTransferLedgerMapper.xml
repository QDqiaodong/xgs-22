<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.example.lightmanager.mapper.BatchTransferLedgerMapper">

    <select id="selectByConditionsPage" resultType="com.example.lightmanager.entity.BatchTransferLedger">
        SELECT * FROM batch_transfer_ledger
        WHERE 1=1
        <if test="operator != null and operator != ''">AND operator LIKE CONCAT('%', #{operator}, '%')</if>
        <if test="startTime != null">AND transfer_time &gt;= #{startTime}</if>
        <if test="endTime != null">AND transfer_time &lt;= #{endTime}</if>
        <if test="batchNo != null and batchNo != ''">AND batch_no LIKE CONCAT('%', #{batchNo}, '%')</if>
        ORDER BY transfer_time DESC
    </select>
</mapper>