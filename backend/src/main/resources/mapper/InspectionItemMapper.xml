<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.example.lightmanager.mapper.InspectionItemMapper">

    <insert id="batchInsert">
        INSERT INTO inspection_item
            (batch_id, light_group_id, group_code, snapshot_zone_id, snapshot_zone_name, snapshot_location)
        VALUES
        <foreach collection="items" item="item" separator=",">
            (#{item.batchId}, #{item.lightGroupId}, #{item.groupCode},
             #{item.snapshotZoneId}, #{item.snapshotZoneName}, #{item.snapshotLocation})
        </foreach>
    </insert>

    <select id="selectByBatchId" resultType="com.example.lightmanager.entity.InspectionItem">
        SELECT * FROM inspection_item
        WHERE batch_id = #{batchId}
        ORDER BY group_code ASC, id ASC
    </select>
</mapper>
